"use client";
import React from 'react';
import styles from './QuickTransfer.module.css';

const QuickTransfer = () => {
  return (
    <div className={styles.quickTransfer}>
      <p className={styles.title}>Quick Transfer</p>
    </div>
  );
};

export default QuickTransfer;
